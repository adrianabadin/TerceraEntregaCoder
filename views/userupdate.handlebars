{{!-- Crear una vista para poder visualizar,
modificar el rol y eliminar un usuario.
Esta vista únicamente será accesible para el administrador del ecommerce --}}
{{!--
<script>
    function deleteUser(id: string) {
        fetch(`/api/users/${id}`, { method: "DELETE", credentials: "include" }).then(res => console.log(res)).catch(e => console.log(e, "error"))
    }
</script> --}}
<form action="/api/users/{{id}}/updaterol" method="post">
    <h4>{{nombre}}</h4>
    <select name="rol">
        <option value="admin">admin</option>
        <option value="premium">premium</option>
        <option value="user">user</option>
    </select>
    <button type="submit">Modificar</button>
</form>
<form action="/api/users/{{id}}/delete" method="post">
    <button type="submit">Eliminar user</button>
</form>
{{!--
<script>
    const eliminarButton = document.getElementById("eliminar")
    eliminarButton.addEventListener("click", deleteUser)
</script> --}}